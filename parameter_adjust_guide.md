# 🎛️ 語音降噪參數調整指南

本指南幫助您理解和調整語音降噪算法的關鍵參數。

---

## 📊 參數快速參考表

| 參數 | 作用 | 範圍 | 調高效果 | 調低效果 |
|------|------|------|----------|----------|
| `g_min_db` | 增益地板 | -10 ~ -25 dB | 背景殘留多，語音自然 | 背景乾淨，可能死寂 |
| `xi_min_db` | 最小先驗 SNR | -20 ~ -35 dB | 微弱語音被切 | 保留細節，殘留噪聲 |
| `q` | 語音缺失機率 | 0.2 ~ 0.7 | 更激進降噪 | 更保守，保留語音 |
| `alpha_g` | 增益平滑 | 0.5 ~ 0.95 | 平滑，可能拖泥帶水 | 乾脆，可能粗糙 |

---

## 🎚️ 調參決策表

| 症狀 | 可能原因 | 建議調整 |
|------|----------|----------|
| 聲音太乾、有機械音 | g_min_db 過低 | 調高 (如 -17 → -14) |
| 背景太吵、降噪無感 | g_min_db 過高 | 調低 (如 -13 → -18) |
| 微弱語音被截斷 | xi_min_db 過高 | 調低 (如 -15 → -25) |
| 殘留太多突發噪聲 | q 過低 | 調高 (如 0.3 → 0.5) |
| 輔音 (f, s, th) 消失 | q 過高 | 調低 (如 0.6 → 0.4) |
| 像在水底、有咕嚕聲 | alpha_g 過低 | 調高 (如 0.7 → 0.9) |
| 語音開頭被切 | alpha_g 過高 | 調低 (如 0.9 → 0.8) |

---

## 📖 參數詳細說明

### 1. g_min_db (增益地板 / Gain Floor)

**物理意義**：算法被允許執行的「最大衰減量」。簡單說，就是「最安靜的時候，要把背景壓多低」。

**參數範圍**：通常在 -10 dB 到 -25 dB 之間。

**直觀理解**：

| 設定 | 效果 |
|------|------|
| 數值越小 (如 -25dB) | 壓得越深，背景越乾淨，但風險是容易把語音的尾音切斷，且容易暴露「音樂噪聲」 |
| 數值越大 (如 -12dB) | 壓得較淺，背景會殘留底噪，但語音聽起來更自然、飽滿，音樂噪聲會被底噪掩蓋住 (Masking Effect) |

**調整建議**：
- 覺得聲音太乾、有機械音、斷斷續續 → 調高 (例如從 -17 改到 -14)
- 覺得背景太吵、降噪無感 → 調低 (例如從 -13 改到 -18)

> 💡 **提示**：V3-2 的 STOI 高就是因為這個值設得很高 (-13dB)

---

### 2. xi_min_db (最小先驗 SNR / Min A Priori SNR)

**物理意義**：這是 V3-4 (Laplacian) 的「致命傷」參數。它告訴算法：「假設最糟糕的情況下，語音信號至少也有這麼強」。

**參數範圍**：通常在 -20 dB 到 -35 dB 之間。

**直觀理解**：它是算法的「靈敏度底線」。

| 設定 | 效果 |
|------|------|
| 設得太高 (如 -10dB) | 算法會變得「眼瞎」，認為任何低於 -10dB 的信號「肯定全是噪聲」，結果就是微弱的語音細節（呼吸聲、輕聲細語）被無情砍掉 |
| 設得太低 (如 -35dB) | 算法會變得非常小心，假設極微弱的信號也可能是語音，這有助於保留細節，但可能會殘留更多噪聲 |

**調整建議**：
- 語音稍微小聲就被吃掉 → 調低 (往負方向，如 -25 改 -30)
- Laplacian MAP 效果極差 → 檢查此值，務必低於 -20

> ⚠️ **警告**：V3-4 之前失敗的主因就是 xi_min_db = -10.0 過高！Rescue 優化後改為 -15.0。

---

### 3. q (語音缺失機率 / A Priori Probability of Speech Absence)

**物理意義**：這是算法的「懷疑程度」。即「我有多大的把握認為當前這個頻率點是純噪聲？」

**參數範圍**：0.0 (完全信任是語音) ~ 1.0 (完全認為是噪聲)。通常在 0.2 ~ 0.7。

**直觀理解**：

| 設定 | 效果 |
|------|------|
| 數值越大 (如 0.7) | 算法更具侵略性，傾向於把東西當噪聲殺掉。適合很吵的環境，但容易誤傷語音 |
| 數值越小 (如 0.3) | 算法更保守，傾向於保留信號。適合安靜環境，保留語音細節 |

**調整建議**：
- 殘留了太多「餐具聲」或「突發噪聲」 → 調高 (更嚴格，如 0.5 改 0.6)
- 語音的輔音 (f, s, th) 不見了 → 調低 (更寬容，如 0.5 改 0.35)

---

### 4. alpha_g (增益平滑因子 / Gain Smoothing Factor)

**物理意義**：控制降噪過程的「反應速度」。

**參數範圍**：0.5 ~ 0.95。

**直觀理解**：

| 設定 | 效果 |
|------|------|
| 數值越高 (如 0.9) | 變化緩慢、平滑。聽起來比較舒服，像有一層柔焦濾鏡，不容易有突兀的刺耳聲，但可能會有「拖泥帶水」的迴音感 |
| 數值越低 (如 0.6) | 反應極快。能迅速切斷噪聲，但聽起來會很「乾」、「脆」，甚至有點粗糙 |

**調整建議**：
- 聲音聽起來像在水底、有咕嚕聲 (Musical Noise) → 調高 (如 0.8 改 0.9)
- 語音的開頭 (Onset) 被切掉、反應遲鈍 → 調低 (如 0.9 改 0.8)

---

## 🏆 Optuna 1000-trial 優化結果 (v2.4.0)

以下是各版本經過 1000 次貝葉斯優化後的最佳參數與性能指標：

| 版本 | PESQ | STOI | segSNR | xi_min_db | g_min_db | alpha_g |
|------|------|------|--------|-----------|----------|---------|
| V4 (IMCRA-OMLSA) | **1.747** | **0.859** | +5.22 dB | -19.0 | -17.0 | 0.87 |
| V3-2 (MMSE-LSA) | 1.738 | **0.859** | +5.12 dB | -22.0 | -13.0 | 0.84 |
| V3-3 (PMMSE) | 1.688 | 0.839 | **+6.06 dB** | -19.0 | -18.0 | 0.80 |
| V3 (MMSE-STSA) | 1.676 | 0.837 | +6.00 dB | -25.0 | -17.0 | 0.80 |
| V3-4 (Laplacian) | 1.539 | 0.840 | +5.10 dB | -15.0 | -18.0 | 0.70 |

> 💡 **說明**：PESQ 範圍 1.0~4.5，STOI 範圍 0~1，segSNR 為改善量 (dB)

---

## 📝 參數調整流程建議

1. **先跑一次基準測試**：使用預設配置處理你的音頻
2. **聆聽並診斷問題**：根據上面的「調參決策表」識別症狀
3. **單一參數調整**：每次只調整一個參數，觀察效果
4. **小步調整**：每次調整 1-2 dB 或 0.05-0.1
5. **記錄最佳值**：找到滿意的參數後記錄下來

---

## 🔗 相關文檔

- [README.md](README.md) - 項目總覽
- [ALGORITHMS_EXPLANATION.md](ALGORITHMS_EXPLANATION.md) - 算法原理詳解
- [config/](config/) - 各版本配置文件
